Flask==2.1.1               # Pour ton application web Flask
gunicorn==20.1.0           # Serveur WSGI pour déployer ton application en production
azure-functions==1.9.0     # SDK pour intégrer Azure Functions
azure-storage-blob==12.10.0 # Pour interagir avec le Blob Storage d'Azure
azure-storage-queue==12.10.0 # Pour gérer les files d'attente (Queues) Azure
Pillow==9.0.0              # Pour le traitement d'images (redimensionnement, etc.)
requests==2.27.1           # Pour faire des requêtes HTTP (si nécessaire)
python-dotenv==0.19.2      # Pour charger des variables d'environnement depuis un fichier .env
